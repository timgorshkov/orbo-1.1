# Hotfix: 4 критичных проблемы (06.11.2025)

## 🎯 **Проблемы:**

### **1. Пустой список "Доступные группы"**
**Статус:** ✅ Не баг, это правильное поведение  
**Причина:** У пользователя несколько Telegram аккаунтов, к организации привязан один, админские права есть у другого.  
**Решение:** Оставляем строгую изоляцию по организациям — показываем только группы аккаунта, привязанного к текущей организации.

---

### **2. Ошибка `participant_audit_log` в логах**
**Статус:** ✅ Исправлено  
**Файлы:** `lib/server/getParticipantDetail.ts`  
**Причина:** Таблица `participant_audit_log` была удалена в миграции 072, но код пытался к ней обращаться.  
**Решение:** Удалён блок кода, пытающийся загрузить audit log.

```typescript
// БЫЛО:
let auditLog: ParticipantAuditRecord[] = [];
try {
  const { data: auditLogData, error: auditError } = await supabase
    .from('participant_audit_log') // ❌ Таблица не существует
    .select('*')
    ...
} catch { ... }

// СТАЛО:
// Audit log feature was removed in migration 072
const auditLog: ParticipantAuditRecord[] = [];
```

---

### **3. Импорт истории из неправильной группы**
**Статус:** ✅ Исправлено (строгая валидация)  
**Файлы:** 
- `lib/services/telegramJsonParser.ts`
- `app/api/telegram/import-history/[id]/parse/route.ts`

**Уязвимости, которые были закрыты:**
1. **Фальсификация импорта:** Можно было импортировать чужую группу, узнав её `chat_id`
2. **Утечка истории:** Можно было загрузить чужие сообщения в свою организацию

**Решение: Строгая валидация `chat_id`**
- JSON импорт: **Обязательная** проверка, что `chat_id` из файла совпадает с группой
- HTML импорт: Предупреждение (нет `chat_id` в формате)

**Как работает:**
1. JSON файл содержит `"id": -1002994446785` (chat_id группы)
2. При парсинге извлекаем `chatId` из JSON
3. Сравниваем с `group.tg_chat_id`
4. Если не совпадает → **ошибка 400** с понятным сообщением

**Сообщение об ошибке:**
```json
{
  "error": "Chat ID mismatch",
  "message": "Файл содержит историю другой группы (ID: -1002994446785). Импорт разрешён только для текущей группы (ID: -4987441578).",
  "hint": "Загрузите правильный файл экспорта для этой группы или выберите другую группу в списке.",
  "importedChatId": -1002994446785,
  "expectedChatId": -4987441578
}
```

**Временные группы для анализа:** Отложено на будущее (после дополнительного анализа безопасности).

---

### **4. Webhook Recovery Too Many Requests**
**Статус:** ✅ Исправлено  
**Файл:** `lib/services/webhookRecoveryService.ts`  
**Причина:** Автоматический recovery вызывался **несколько раз подряд** (несколько webhook-запросов одновременно), Telegram блокировал rate limit.  

**Решение:**
1. **Глобальная блокировка:** Если recovery уже идёт — новые попытки отклоняются
2. **Игнорирование Telegram rate limit:** Если Telegram вернул "Too Many Requests" — не считаем как failed attempt

```typescript
class WebhookRecoveryService {
  private activeRecoveries: Set<string> = new Set(); // ⭐ Новая блокировка
  
  async recoverWebhook(botType: 'main' | 'notifications', reason: string): Promise<boolean> {
    // Проверяем, не идёт ли уже recovery
    if (this.activeRecoveries.has(botType)) {
      console.warn(`[Webhook Recovery] Recovery already in progress for ${botType} bot`);
      return false;
    }
    
    // Блокируем новые попытки
    this.activeRecoveries.add(botType);
    
    try {
      // ... восстановление webhook ...
    } catch (error: any) {
      // Если Telegram rate limit — это не наша проблема
      if (error.message?.includes('Too Many Requests')) {
        console.warn(`[Webhook Recovery] ⏳ Telegram rate limit hit, will retry later`);
        return false; // НЕ записываем как failed attempt
      }
      // ... остальная обработка ошибок ...
    } finally {
      // Снимаем блокировку
      this.activeRecoveries.delete(botType);
    }
  }
}
```

---

## 📝 **Изменённые файлы:**

1. `lib/server/getParticipantDetail.ts` — Удален audit log
2. `lib/services/webhookRecoveryService.ts` — Блокировка recovery
3. `lib/services/telegramJsonParser.ts` — Добавлен `chatId` в результат
4. `app/api/telegram/import-history/[id]/parse/route.ts` — Валидация `chat_id`

---

## 🧪 **Тестирование:**

### **Проблема 2 (Audit Log):**
- ✅ Открыть профиль участника → Не должно быть ошибки в логах Vercel

### **Проблема 3 (Импорт):**
1. ✅ Загрузить JSON файл **правильной** группы → Импорт успешен
2. ✅ Загрузить JSON файл **другой** группы → Ошибка "Chat ID mismatch"
3. ✅ Проверить, что новые участники появились в списке (если были созданы)
4. ✅ Проверить, что сообщения появились в профиле участника (вкладка "Активность")

### **Проблема 4 (Webhook Recovery):**
- ✅ Следить за логами Vercel → Не должно быть множественных вызовов `setWebhook` подряд

---

## 🚀 **Деплой:**

```bash
git add .
git commit -m "Hotfix: audit log error, import validation, webhook recovery rate limit"
git push
```

---

## ✅ **Checklist после деплоя:**

- [ ] Проверить логи Vercel на отсутствие ошибки `participant_audit_log`
- [ ] Попробовать импортировать JSON файл **правильной** группы
- [ ] Попробовать импортировать JSON файл **другой** группы (должна быть ошибка)
- [ ] Проверить, что webhook recovery больше не вызывается массово
- [ ] Проверить, что новые участники из импорта появляются в списке
- [ ] Проверить, что сообщения из импорта отображаются в профиле участника

---

**Дата:** 06.11.2025  
**Автор:** Assistant + Timur  
**Приоритет:** Критичный (безопасность + UX)


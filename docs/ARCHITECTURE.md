# Архитектура Orbo

> **Версия:** 3.0 (Январь 2026)
> 
> Этот документ описывает актуальную архитектуру системы после миграции с Supabase.

## 🏗️ Обзор архитектуры

```
┌─────────────────────────────────────────────────────────────────┐
│                         Клиенты                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │
│  │  Web     │  │ MiniApp  │  │  Telegram│  │   Cron Jobs      │ │
│  │  Browser │  │ Telegram │  │    Bot   │  │   (Internal)     │ │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────────┬─────────┘ │
└───────┼─────────────┼─────────────┼─────────────────┼───────────┘
        │             │             │                 │
        ▼             ▼             ▼                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Next.js 15 (App Router)                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  API Routes (/api/*)                                      │   │
│  │  ┌────────────┐ ┌────────────┐ ┌───────────────────────┐ │   │
│  │  │ Auth       │ │ Events     │ │ Telegram Webhook      │ │   │
│  │  │ /api/auth  │ │ /api/events│ │ /api/telegram/webhook │ │   │
│  │  └────────────┘ └────────────┘ └───────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Services Layer                                           │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐ │   │
│  │  │ Auth     │ │ Events   │ │ Telegram │ │ Notifications│ │   │
│  │  │ Service  │ │ Service  │ │ Service  │ │ Service      │ │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────┘ │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────────────┬─────────────────────────────────┘
                                │
        ┌───────────────────────┼───────────────────────┐
        │                       │                       │
        ▼                       ▼                       ▼
┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐
│   PostgreSQL     │   │  Selectel S3     │   │   External APIs  │
│   Database       │   │  Storage         │   │                  │
│   (Selectel)     │   │                  │   │  • Telegram API  │
│                  │   │  • org-logos/    │   │  • OpenAI API    │
│  190+ Tables     │   │  • event-covers/ │   │  • Unisender     │
│  128+ RPC Funcs  │   │  • photos/       │   │                  │
└──────────────────┘   └──────────────────┘   └──────────────────┘
```

---

## 📊 Компоненты системы

### 1. Frontend (Next.js 15)

**Технологии:**
- Next.js 15 с App Router
- React 19
- TypeScript 5.x
- TailwindCSS 3.x
- shadcn/ui компоненты

**Структура страниц:**
```
app/
├── (auth)/           # Публичные auth страницы
│   ├── signin/
│   └── signup/
├── app/[org]/        # Защищённые страницы (требуют auth)
│   ├── dashboard/
│   ├── telegram/
│   ├── events/
│   └── members/
├── p/[org]/          # Публичные страницы организации
├── tg-app/           # Telegram MiniApp
├── site/             # Лендинг orbo.ru
└── api/              # API routes
```

### 2. Authentication (NextAuth.js v5)

**Провайдеры:**
- Google OAuth
- Yandex OAuth
- Email magic links (Unisender)
- Telegram 6-digit codes (custom)

**Архитектура:**
```typescript
// lib/auth/unified-auth.ts
export async function getUnifiedSession(): Promise<UnifiedSession | null>
export async function getUnifiedUser(): Promise<UnifiedUser | null>
export async function requireAuth(): Promise<UnifiedSession>
```

**JWT Session:**
- Хранится в httpOnly cookie (`authjs.session-token`)
- Содержит: user.id, user.email, user.name
- Время жизни: 30 дней с автообновлением

### 3. Database (PostgreSQL 16)

**Хостинг:** Selectel Managed Database

**Клиент:**
```typescript
// lib/db/postgres-client.ts
export function getPostgresClient(): DbClient
```

**Особенности:**
- 190+ миграций в `db/migrations/`
- 128+ RPC функций
- Без RLS (контроль доступа в коде)
- Connection pooling через PgBouncer

**Ключевые таблицы:**
- `users` — пользователи системы
- `organizations` — организации
- `memberships` — связь user↔org с ролями
- `participants` — участники групп
- `telegram_groups` — подключённые группы
- `events` — события
- `notification_rules` — правила уведомлений

### 4. Storage (Selectel S3)

**Конфигурация:**
```env
STORAGE_PROVIDER=s3
SELECTEL_ENDPOINT=https://s3.storage.selcloud.ru
SELECTEL_BUCKET=orbo-materials
```

**Buckets/Paths:**
- `org-logos/` — логотипы организаций
- `event-covers/` — обложки событий
- `photos/` — фотографии участников
- `app-logos/` — иконки приложений

**Интерфейс:**
```typescript
// lib/storage/index.ts
export function getStorage(): StorageProvider
export async function uploadOrgLogo(orgId: string, file: Buffer): Promise<string>
```

### 5. Telegram Integration

**Компоненты:**
- Основной бот: `@orbo_community_bot`
- Webhook: `/api/telegram/webhook`

**Функции:**
- Синхронизация участников групп
- Отправка уведомлений
- Обработка команд
- 6-digit авторизационные коды

### 6. External Integrations

| Сервис | Назначение | Endpoint |
|--------|------------|----------|
| Telegram Bot API | Боты и webhook | api.telegram.org |
| OpenAI API | AI-анализ | api.openai.com (через прокси) |
| Unisender | Email отправка | go1.unisender.ru |
| Hawk.so | Мониторинг ошибок | hawk.so |

---

## 🔐 Безопасность

### Аутентификация
- JWT токены в httpOnly cookies
- PKCE flow для OAuth
- Секретный токен для Telegram webhook

### Авторизация
- Проверка membership в каждом API route
- Роли: owner, admin, member, guest
- Guard функции в `lib/orgGuard.ts`

### Данные
- SSL/TLS для всех соединений
- Параметризованные SQL-запросы
- Валидация через Zod

---

## 🚀 Deployment

### Docker Compose (Production)
```yaml
services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - postgres
  postgres:
    image: postgres:16
    volumes:
      - postgres_data:/var/lib/postgresql/data
```

### Health Checks
- `/api/health` — статус приложения
- `/api/healthz` — liveness probe для Kubernetes

---

## 📁 Структура проекта

```
orbo-1.1/
├── app/                    # Next.js App Router
│   ├── api/                # API routes
│   ├── app/[org]/          # Protected org pages
│   ├── p/[org]/            # Public org pages
│   ├── tg-app/             # Telegram MiniApp
│   └── site/               # Landing pages
├── components/             # React components
│   ├── ui/                 # Base UI (shadcn)
│   └── ...                 # Feature components
├── lib/
│   ├── auth/               # Auth utilities
│   ├── db/                 # Database client
│   ├── storage/            # Storage provider
│   ├── services/           # Business logic
│   └── ...
├── db/
│   └── migrations/         # SQL migrations
├── deploy/                 # Docker & deployment
├── docs/                   # Documentation
└── memory-bank/            # AI context files
```

---

## 🔄 Data Flow Examples

### 1. User Login (OAuth)
```
Browser → NextAuth.js → Google/Yandex
       ← JWT cookie ←
Browser → API Route → getUnifiedSession()
                    → PostgreSQL (check user)
       ← Response ←
```

### 2. Telegram Message Processing
```
Telegram → /api/telegram/webhook
         → Verify secret token
         → PostgresClient.from('activity_events').insert()
         → Check notification rules
         → Send notifications if needed
```

### 3. File Upload
```
Browser → /api/.../upload
        → getStorage()
        → SelectelS3.upload()
       ← Public URL ←
        → PostgreSQL (update record)
```

---

## 📚 Связанные документы

- `docs/SETUP_GUIDE.md` — Руководство по настройке
- `docs/COMPREHENSIVE_PRD.md` — Полное описание продукта
- `deploy/STEP_BY_STEP_GUIDE.md` — Пошаговый деплой
- `memory-bank/techContext.md` — Технический контекст для AI

---

**Дата обновления:** 20 января 2026
